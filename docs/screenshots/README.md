# README screenshots

These PNGs are referenced from the root **`README.md`**. The committed files are **solid-color placeholders** (9:16 aspect) so the repo renders cleanly on GitHub before you add real UI captures.

## Replace with your own (recommended)

Capture from a browser running `streamlit run app.py` (narrow window ≈ phone width looks best):

| File | Suggested content |
|------|-------------------|
| `01-upload.png` | **Step 1 · Upload** — file picker + supported formats caption |
| `02-options.png` | **Step 2 · Options** — music, vertical framing radio, subtitles |
| `03-generate.png` | **Step 3 · Generate** — preview + **Generate Short** button |
| `04-results.png` | **Step 4 · Results** — download MP4/JSON + metadata summary |

**Size:** ~1080×1920 or half (e.g. 540×960); keep 9:16 if possible. Export as PNG, overwrite the files here, commit.

**Tools:** Snipping Tool / ShareX (Windows), Screenshot (macOS), or browser devtools device frame.
